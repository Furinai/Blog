<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.citru.blog.mapper.SentenceMapper">

    <insert id="insertSentence">
        insert into sentence(note, content, create_time)
        values (#{note}, #{content}, #{createTime})
    </insert>

    <select id="selectSentence" resultType="Sentence">
        select *
        from sentence
        order by create_time desc
        limit 1
    </select>
</mapper>